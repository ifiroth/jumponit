<?php

namespace JOI\Service;

class ActionManager {

    public static function logAction($action, $id, $valueBefore, $valueAfter, $selection) {
        // TODO : log action into db

        return true;
    }

    public static function getLoggedAction($action, $id) : array {
        // TODO : define getLoggedAction()

        return [];
    }
}
